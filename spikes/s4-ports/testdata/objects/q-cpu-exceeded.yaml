apiVersion: v1
kind: Pod
metadata:
  name: q-cpu-exceeded
  namespace: s4
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
    resources:
      requests: {cpu: 700m, memory: 128Mi}
      limits: {cpu: 800m, memory: 256Mi}
