apiVersion: v1
kind: Pod
metadata:
  name: lr-under-min
  namespace: s4
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
    resources:
      requests:
        cpu: 20m
        memory: 128Mi
      limits:
        cpu: 20m
        memory: 256Mi
