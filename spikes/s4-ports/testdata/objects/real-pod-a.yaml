apiVersion: v1
kind: Pod
metadata:
  name: real-pod-a
  namespace: s4
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: 500m, memory: 512Mi}
